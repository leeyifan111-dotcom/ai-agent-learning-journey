001 agent 的本质是「在环境中持续用工具推理决策直到目标达成」，他是有状态的(有上下文记忆功能)、感知外界的(通过爬取web工具等)、多步推理的(多轮循环，将复杂任务拆分)、自主性(自主选择工具请求调用，可自动终止循环)、自适应的(根据中间结果调整策略)，而不是无状态的、单一问答的、被动响应的。
021 agent 通过工具定义的方式，可以让llm生成json去请求调用工具，一般来说工具的组成分为:name / description / input_schema，除此之外，需要告诉 LLM 有哪些工具（schema），并在本地维护"工具名 → 函数"的派发表，调用结果回填后 LLM 再根据返回继续推理。
006 agent 总是有loop循环，为了防止无限循环，需要有限制条件：
        可恢复 -> 重试，模型降级，工具兜底等等限制条件
            重试次数有限，一般来说像网络波动、超时等错误可以重试，但超过一定次数后，agent会终止循环；模型降级一般适用于返回该模型不支持的错误，或者模型服务出问题，需要切换模型，一般配合超时使用；
        不可恢复 -> 终止条件、优雅推出
            权限错误、工具异常等
        除错误处理外，还需要确定终止条件（max_turns / token 预算 / 重复任务检测）作为硬上限，哪怕一切正常运行也强制停下。
022 agent tools 定义三要素:name,description,input_schema，三者之间
相辅相成，llm可以根据name和input_schema推断使用哪个工具，也能根据description推断应该请求哪个工具，如果其中一个错误，另外两个正确，llm也是能够正确推断出应该请求调用什么工具，llm会根据tools定义生成json调用参数，但定义不清晰也会有错误的风险，高质量的定义往往能减少llm试错成本。