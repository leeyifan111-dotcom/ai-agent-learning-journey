011 rag 本质：把"长期记忆"做成"检索后喂"，基于外部的知识库，在llm回答之前，可以支持llm检索以获取其不知道的知识辅助回答用户的问题，可以有效降低知识截止、幻觉、缺乏私有/领域知识的回答。
    知识截止： llm只能获取到其训练截止日期之前的知识，在之后的相关的实时新闻等事实都是未知的，llm对于未知的问题有可能会凭空编造，虚假回答用户问题。
    幻觉： 信息不足导致的编造，可减少，但参数化记忆+采样随机性带来的幻觉根除不了。
    缺乏私有/领域知识： 对于一些企业内部文档、私有数据库或最新的领域知识，llm无法在训练中获取，无法回答。

    有了rag也不是绝对的能够避免以上三种现象
    1. 受知识库质量影响，知识库质量差导致lln拿到的信息质量不够高。
    2. 受检索质量影响，检索到的知识可能与用户的问题无关。
    3. llm理解能力，如果知识库再好，llm能力不足那对于用户的问题也不会有高质量的回答。

012 rag pipeline 拆成两条线：
    离线（indexing）：文档加载 → 分块 → embedding → 存向量库
    在线（query）：query embedding → 检索 → 生成

    护栏（每个环节都要有）：
        检索召回：top-K 限制；超过上下文窗口时重排序，保留高分切片（top n）
        检索返回 0 条：
            1) 知识库本身缺失对应内容 → 补语料
            2) 清洗/分块策略不合理，语义割裂 → 调整切分策略
            3) 向量库存储/元数据损坏 → 排查 index 状态
            4) 查询优化三件套：
                a. 重写 query 提升语义匹配度(Query Rewriting)
                b. 拆复杂问题为多个子问题(Query Decomposition)
                c. 生成假设性回答(HyDE)
            5) 适当放宽 top-K 阈值
        embedding 超时：
            1) 切片过大转向量超时 → 优化切分策略
            2) 紧急降级为关键词检索(BM25)兜底
            3) 提问 embedding 模型必须跟存储时一致，否则检索全错
