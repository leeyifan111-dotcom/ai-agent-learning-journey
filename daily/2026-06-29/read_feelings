013 常见切分策略：
    1. 固定长度分块：设置chunk_size和overlap，就会默认按照这个长度分块，优点是实现简单，块大小均匀，便于索引优化；缺点是句子很有可能被截断，非常容易丢失语义和完整性。适用于快速原型验证和简短内容文档。
    2. 递归分块：设置chunk_size和overlap，会按照段落、换行、标点符号、空格等切分标记一次尝试不超过chunk_size的切分，优点是减少句子被截断的风险，实现简单计算成本低，相对固定长度分块能提高语义的完整性；缺点是不同文档需要不同的分隔符集，没有考虑语义相似度。
    3. 语义分块：对每个句子生产embedding，计算相邻句子的余弦相似度，在相似度急剧下降(主题切换)的地方分块。优点是利用语义相似度能够极大可能保障语义完整性，recall能到90%以上，块大小自适应内容；缺点是每个句子都需要生产embedding，实现起来计算复杂成本高，切分慢，块内容大小不一致不利于生成索引。

    一般来说使用递归分块就能够适应大多数场景，对于chunk_size和overlap的大小需要基于数据集去测算出最优选项，一般从 512 token和 50 token开始调试。

014 个人一般用chroma或者Qdrant(小于1GB免费)，中型团队用Weaviate，大型团队用Pinecone(代托管，要钱)、Milvus(自托管，免费)

015 embedding 模型三选：MiniLM(all-MiniLM-L6-v2, 384维) / text-embedding-3-small(1536维, 可降维) / bge-m3(1024维)。
     v0.1 用硅基流动 BAAI/bge-m3(1024维, 中文最佳, 免费额度够用)。
     关键教训：嵌入模型必须跟存储时一致(Chroma 残留 384 维直接报错)；英文模型嵌中文=检索全废。