016 语义检索和关键词检索没有哪个是绝对的好，各有各的好处，关键词检索能够匹配一些专业术语，而语义检索就做不好这点，语义检索能够将近义词结合起来比如(轿车和小车、水果和香蕉)，而关键词检索就做不到这点，所以一般在生产中都是用的混合检索。
    常用混合检索模型：
        1. Reciprocal Rank Fusion（RRF）模型：将同一份文档语义检索和关键词检索模型的排名按照算法 score=(1/(ranka+k)+1/(rankb+k))，k一般取常量60，得分越高证明文档匹配度越高，优点就是不用调权重，非常鲁棒。
        2. 线性组合（Weighted Scoring）模型：这个就是给语义检索得分和关键词检索得分分别一个权重，业务场景更依赖语义理解时，就提高语义检索的权重，反之则提高关键词检索权重，然后计算加权得分，这种模型的话灵活性会更强一点，在特殊场景经过调参表现效果会比rrf更好。
    除了混合模型之外，在实际运用场景中一般会有两段式架构，第一层用混合模型检索排名取top 100，第二层用Cross-Encoder精排名取top 10，原因是混合模型对于query-document关系评估缺乏深层语义理解，而Cross-Encoder则能很好地处理这一点，代价就是计算成本比较昂贵，所以为了节约成本需要基于top 100 来选取top10。



017 rerank 本质：粗排（Bi-Encoder/BM25 混合检索，取 top-100）
                 精排（Cross-Encoder 逐对打分，取 top-10）的两段式架构。

    核心差异不在"模型更大"，而在交互方式：
        Bi-Encoder = 分别嵌 query 和 doc，再余弦比对——doc 向量可离线预计算，在线只等 query 编码
        Cross-Encoder = query+doc 拼一起现场跑模型——不能预计算，每条都得活着跑，所以只能跑 top-K

    为什么是 100 和 10？不是魔法数字——LLM 上下文窗口锁死精排输出上限（3-5 chunk），
    粗排候选不能太小（漏关键文档），精排不能太大（Cross-Encoder 每条 ~30ms，100 条=3 秒）。
    v0.1 选 bge-reranker-v2-m3（本地，免费，中文最好），不选 Cohere（要注册+付费）。