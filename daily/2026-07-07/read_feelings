049 cot 是线性思考，tot是树形思考
    cot分为 zero-shot-cot和few-shot-cot，区别是一个给上具体线性思考例子，一个只给thought step by step提示词
    cot优化：self-consistency 将cot过程n次循环，生成n次答案，再选取其中最多命中次数答案输出，目的是避免模型出现小概率错误思考

    tot是每一步骤都生成多个选项，形成多个推理状态，给每个状态打分，选取最高分top n选项继续向下探索，如果失败还可以回溯到中间过程。
    搜索策略：
    - BFS：广度优先，保留多个分支并行探索。
    - DFS：深度优先，一条路走到底，失败再回溯。

050 任务拆分分为llm驱动、程序化拆分、层级拆分、自适应拆分、TDAG拆分
    其中稳定性最高的是程序化拆分，这就像一种workflow，给出具体拆分步骤
    llm实现最简单，但是稳定性不高，灵活性高
    层级拆分就是采用递归方式，不断把任务拆成更小的子任务，直到每个任务都可以直接执行。
    自适应拆分根据执行反馈动态调整 Plan，先执行，再观察结果，必要时重新拆分和规划。
    TDAG拆分就是把任务组织成 DAG，存在依赖关系的顺序执行，没有依赖关系的任务可以并行执行，适合多agent，示例：
    dag = {
        "搜索A": {"depends_on": []},
        "搜索B": {"depends_on": []},       # 与搜索A并行
        "分析": {"depends_on": ["搜索A", "搜索B"]},  # 等两个搜索完成
        "报告": {"depends_on": ["分析"]},
    }